/**
 * Percentage (0-1) of coffee fill at which the text color should switch
 * from brown (contrast against empty cup) to cream (contrast against coffee).
 */
export const COLOR_SWITCH_PERCENTAGE = 0.6;

/**
 * Used to clamp the duration to a maximum number of seconds.
 * This duration represents the time: 99:59:59
 */
export const MAX_DURATION_IN_SECONDS = 359999;
